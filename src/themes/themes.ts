import { extendTheme } from '@chakra-ui/react';

const customThemes = extendTheme({
  colors: {
    main: {
      100: '#9ae6b4'
    },
    richText: {
      100: '#1c1c1c'
    }
  }
});
export default customThemes;
